@model IEnumerable<Logistics.Models.Organization>
@{
    Layout = "~/Views/Shared/_LayoutOrganizations.cshtml";
}

<div class="row page-titles">

    <div class="col-md-6 col-8 align-self-center">
        <h3 class="text-themecolor m-b-0 m-t-0">Semua Organisasi</h3>
        <ol class="breadcrumb">
            <li class="breadcrumb-item active">Kelola Organisasi</li>
        </ol>
    </div>
</div>


<div class="card">
    <div class="card-body">
        @*<p><a class="btn btn-info" href="@Url.Action("AddFreeEvent")">Tambah Event Gratis</a> | <a class="btn btn-info" href="@Url.Action("AddPaidEvent")">Tambah Event Berbayar</a> | <a class="btn btn-info" href="@Url.Action("AddSupportingEvent")">Tambah Event Support</a></p>*@
        <div class="table-responsive m-t-40">
            <table id="myTable" class="table table-bordered table-striped">
                <thead class="table-header">
                    <tr class="color-table">
                        <th>Full Name</th>
                        <th>Email</th>
                        <th>Institution</th>
                        <th>Province</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.FullName</td>
                            <td>@item.Email</td>
                            <td>@item.Institution</td>
                            <td>@item.Province.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Styles
{
    <link rel="stylesheet" href="~/assets/css/custom.css">
}
@section Scripts{
    <script src="~/assets/vendor/datatables/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#myTable').DataTable();

        });
    </script>
}


